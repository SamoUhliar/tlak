import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.grizzly.tlak',
  appName: 'tlak',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
